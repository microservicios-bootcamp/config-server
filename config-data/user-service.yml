server:
  port: 8092
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    uri: mongodb+srv://sa:<password>@cluster0.iyabt.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
management:
  endpoints:
    web:
      exposure:
        include: "*"